---
to: src/pages<%= path %>.tsx
---
import type { HTMLAttributes } from 'react'
import { useI18n } from 'next-rosetta'
import cn from 'classnames'
import type { SiteLocale } from '@shared/i18n'
import s from './index.module.css'

export interface <%= h.changeCase.pascal(name) %>PageProps extends HTMLAttributes<HTMLDivElement> {}

const <%= h.changeCase.pascal(name) %>Page = ({ className, ...props }: <%= h.changeCase.pascal(name) %>PageProps): JSX.Element => {
	const i18n = useI18n<SiteLocale>()
	const { t } = i18n

	return (
    <div className={cn(s.component, className)} {...props}>
      {t('<%=h.changeCase.lowerCase(name)%>.hero.title')}
    </div>
	)
}

export default <%= h.changeCase.pascal(name) %>Page
