---
to: src/<%= folder %>/<%= h.inflection.dasherize(name) %>/index.tsx
---
import { memo } from 'react'
import type { FC, HTMLAttributes } from 'react'
import cn from 'classnames'
import s from './index.module.css'

export interface <%= h.changeCase.pascal(name) %>Props extends HTMLAttributes<HTMLDivElement> {}

const <%= h.changeCase.pascal(name) %>: FC<<%= h.changeCase.pascal(name) %>Props> = ({ className, ...props }): JSX.Element => {
  return (
    <div className={cn(s.component, className)} {...props}>
      <%= h.changeCase.pascal(name) %>
    </div>
  )
}

// If you leave memo, the i18n texts
// will not be displayed when switching languages
export default memo(<%= h.changeCase.pascal(name) %>)
